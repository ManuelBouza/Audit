<openerp>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="audit.list">
      <field name="name">audit list</field>
      <field name="model">audit.audit</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="audit.action_window">
      <field name="name">audit window</field>
      <field name="res_model">audit.audit</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="audit.action_server">
      <field name="name">audit server</field>
      <field name="model_id" ref="model_audit_audit"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="audit" id="audit.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="audit.menu_1" parent="audit.menu_root"/>
    <menuitem name="Menu 2" id="audit.menu_2" parent="audit.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="audit.menu_1_list" parent="audit.menu_1"
              action="audit.action_window"/>
    <menuitem name="Server to list" id="audit" parent="audit.menu_2"
              action="audit.action_server"/>
    -->
  </data>
</openerp>